spring:
  application:
    name: AccountMS

  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: SYSTEM
    password: oracle
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:4567/eureka/
    fetch-registry: true
    register-with-eureka: true

# Custom Mock Account Number Settings
account:
  number:
    prefix: "ACC"     # Example: ACC001234567
    length: 12        # Digits after prefix

# KYC Microservice Endpoint for verification
kyc:
  base-url: http://localhost:8083/api/kyc-verification

management:
  endpoints:
    web:
      exposure:
        include: health,info
